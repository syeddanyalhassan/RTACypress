{
  "dependencies": {
    "cypress": "^13.14.2"
  },
  "scripts": {
    "test": "npx cypress run --spec cypress/e2e/company.cy.js",
    "tester": "npx cypress run --env --browser=chrome --headed --spec cypress/e2e/company.cy.js --reporter mocha-allure-reporter",
    "allure:report": "allure generate allure-results --clean -o allure-report && allure open allure-report"
 
  }
}
